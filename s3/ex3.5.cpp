#include<iostream>
#include<string>
using namespace std;
void concatenate(){
    string total,s;
    while(cin>>s)
	total+=s;
    cout<<total;
}

void concatenate_with_space(){
    string total,s;
    while(cin>>s)
	total=total+" "+s;
    cout<<total
	total+=s;
    cout<<total;
}

void concatenate_with_space(){
    string total,s;
    while(cin>>s)
	total=total+" "+s;
    cout<<total
	total+=s;
    cout<<total;
}

void concatenate_with_space(){
    string total,s;
    while(cin>>s)
	total=total+" "+s;
    cout<<total
	total+=s;
    cout<<total;
}

void concatenate_with_space(){
    string total,s;
    while(cin>>s)
	total=total+" "+s;
    cout<<total;;;;
